package com.hoffrogge.lehreinheit02;

public class WuerfelUebung {

    public static void main(String[] args) {

	/*
	 * ...
	 * =============================================================================
	 * AUFGABE 1: Erstelle ein Objekt vom Typ Wuerfel ohne Werte
	 * =============================================================================
	 */

	/*
	 * =============================================================================
	 * AUFGABE 2: Erstelle ein weiteres Objekt vom Typ Wuerfel mit einer Zahl als
	 * Argument
	 * =============================================================================
	 */

	/*
	 * =============================================================================
	 * AUFGABE 3: Erstelle ein weiteres Objekt vom Typ Wuerfel mit einem Text als
	 * Argument
	 * =============================================================================
	 */

	/*
	 * =============================================================================
	 * AUFGABE 4: Erstelle ein weiteres Objekt vom Typ Wuerfel mit einer Zahl und
	 * einem Text als Argument
	 * =============================================================================
	 */

	/*
	 * =============================================================================
	 * AUFGABE 5: Gebe alle Objekte mit ihren Werten mit Hilfe von
	 * System.out.println(...) und den Gettern aus
	 * =============================================================================
	 */

	/*
	 * =============================================================================
	 * AUFGABE 6: Nutze die Setter um die Werte aller Objekte zu aendern und gebe
	 * sie erneut aus
	 * =============================================================================
	 */
    }
}